---
const { image, name, role } = Astro.props;
---

<div class="text-center my-2">
    <img src={image} class="m-auto w-48 rounded-full aspect-square shadow-xl" />
    <h3 class="text-2xl font-bold">{name}</h3>
    <p>{role}</p>
</div>
